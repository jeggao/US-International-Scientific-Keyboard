.obsidian/

# Python tooling under tools/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
# `pip install -e .` writes this next to the package. Note that `dist/` is NOT
# ignored: it holds the generated Linux and macOS layouts, which are shipped.
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# MSKLC build output
*.dll
*.inf
*.cat
*.msi
setup.exe
